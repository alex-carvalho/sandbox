services:
  loki:
    image: grafana/loki:3.7.2
    ports:
      - "3101:3100"
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - loki-net

networks:
  loki-net:
    driver: bridge
