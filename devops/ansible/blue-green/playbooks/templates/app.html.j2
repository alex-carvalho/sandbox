<!DOCTYPE html>
<html>
<head>
  <title>Blue-Green POC — {{ version }} ({{ slot | capitalize }})</title>
  <style>
    body { font-family: Arial, sans-serif; background: {{ bg_color }}; color: #cdd6f4;
           display: flex; justify-content: center; align-items: center;
           min-height: 100vh; margin: 0; }
    .card { background: #1e1e2e; padding: 40px; border-radius: 12px;
            box-shadow: 0 0 30px rgba(0,0,0,0.5); max-width: 480px; width: 100%; }
    h1 { margin-top: 0; }
    .slot { display: inline-block; background: {{ slot_color }}; color: #1e1e2e;
            font-weight: bold; padding: 4px 14px; border-radius: 20px; font-size: 1.1em; }
    .version { display: inline-block; background: #f9e2af; color: #1e1e2e;
               font-weight: bold; padding: 4px 14px; border-radius: 20px; font-size: 1.1em; }
    .block { background: #313244; border-radius: 6px; padding: 12px 16px;
             font-family: monospace; font-size: 0.9em; line-height: 1.8; margin-top: 16px; }
    .label { color: #94e2d5; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Blue-Green Deploy POC</h1>
    <span class="slot">{{ slot | upper }}</span>&nbsp;<span class="version">{{ version }}</span>
    <div class="block">
      <span class="label">slot    :</span> {{ slot }}<br>
      <span class="label">version :</span> {{ version }}<br>
      <span class="label">host    :</span> {{ inventory_hostname }}
    </div>
  </div>
</body>
</html>
