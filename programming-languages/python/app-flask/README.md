# Flask Application with Kubernetes & Helm

Simple Flask app to run on k8s with helm

### Run Locally

```bash
./run-local.sh
```

Access at `http://localhost:5000`

### Deploy to Kubernetes (Kind)

```bash
./deploy-kind.sh
```